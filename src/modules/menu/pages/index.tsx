import { Box } from "@mui/material";
import { FC } from "react";
import MenuList from "../components/MenuLlist";

const MenuPage: FC = () => {
  return (
    <Box>
      <MenuList />
    </Box>
  );
};

export default MenuPage;
